enum Meal {
  BREAKFAST
  SECOND_BREAKFAST
  LUNCH
  DINNER
}

extend type Query @resolve(class: "EnumResolver") {
  bestMeal: Meal!
  echoEnum(value: Meal!): Meal!
  echoListEnum(value: [Meal]!): [Meal]!
}
